export enum SenderType {
  CLIENT = "CLIENT",
  ADMIN = "ADMIN",
}

export enum ClientType {
  AUTHENTICATED = "AUTHENTICATED",
  ANONYMOUS = "ANONYMOUS",
}

export enum UserRole {
  CLIENT = "CLIENT",
  ADMIN = "ADMIN",
}
